import { StyleSheet } from "react-native";

export default StyleSheet.create({

    container: {
        flex: 1,
        backgroundColor: "#fff",
        padding: 20
    },

    mt: {
        marginTop: 10
    },

    mb: {
        marginBottom: 10
    },

    center: {
        justifyContent: "center",
        alignItems: "center"
    },

    row: {
        flexDirection: "row"
    },

    shadow: {
        elevation: 5,
        shadowColor: "#000"
    }

});